<?xml version="1.0" encoding="UTF-8"?>
<project name="${project.name}">

	<bannerLeft>
		<name>${project.name}</name>
	</bannerLeft>

	<publishDate format="yyyy-MMMM-dd HH:mm:ss" />

	<body>

		<!-- breadcrumb nav -->
		<links>
			<item name="Githup" href="${project.scm.url}" />
		</links>

		<!-- side bar menu -->
		<menu name="About">
			<item name="Home" href="index.html" />
			<item name="History" href="history.html" />
		</menu>

		<!-- reports -->
		${reports}

	</body>

</project>